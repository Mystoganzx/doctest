{
  "version": 2,
  "builds": [
    {
      "src": "server.js",  // Confirme se "index.js" é o nome do seu arquivo principal
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js"  // Confirme se "index.js" é o nome do seu arquivo principal
    }
  ]
}
